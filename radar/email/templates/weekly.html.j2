<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>OSS Radar Weekly Digest</title>
  <style>
    body {
      margin: 0; padding: 0;
      background: #0d1117;
      color: #e6edf3;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, sans-serif;
      font-size: 14px;
      line-height: 1.6;
    }
    a { color: #58a6ff; text-decoration: none; }
    a:hover { text-decoration: underline; }

    .wrapper { max-width: 700px; margin: 0 auto; padding: 24px 16px; }

    /* Weekly banner with different gradient */
    .header {
      background: linear-gradient(135deg, #0d1117 0%, #1a237e 50%, #0d1117 100%);
      border: 1px solid #30363d;
      border-radius: 12px;
      padding: 36px 28px;
      margin-bottom: 24px;
      text-align: center;
    }
    .header h1 {
      margin: 0 0 8px;
      font-size: 28px;
      font-weight: 700;
      background: linear-gradient(90deg, #79c0ff, #d2a8ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .header .week-banner {
      display: inline-block;
      margin-top: 8px;
      padding: 4px 16px;
      background: rgba(88, 166, 255, 0.15);
      border: 1px solid #1f6feb;
      border-radius: 20px;
      color: #58a6ff;
      font-size: 13px;
      font-weight: 600;
    }
    .header .subtitle { color: #8b949e; font-size: 13px; margin: 8px 0 0; }

    /* Stats row */
    .stats-row {
      display: flex;
      gap: 12px;
      margin-bottom: 24px;
      flex-wrap: wrap;
    }
    .stat-card {
      flex: 1;
      min-width: 120px;
      background: #161b22;
      border: 1px solid #30363d;
      border-radius: 8px;
      padding: 14px 16px;
      text-align: center;
    }
    .stat-card .stat-value {
      font-size: 22px;
      font-weight: 700;
      color: #79c0ff;
    }
    .stat-card .stat-label {
      font-size: 11px;
      color: #8b949e;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Entry card */
    .entry {
      background: #161b22;
      border: 1px solid #30363d;
      border-radius: 10px;
      padding: 20px 22px;
      margin-bottom: 14px;
    }
    .entry-header { display: flex; align-items: flex-start; gap: 10px; }
    .rank-badge { font-size: 22px; flex-shrink: 0; line-height: 1.4; }
    .entry-title { font-size: 16px; font-weight: 600; color: #e6edf3; }
    .entry-meta {
      margin-top: 10px;
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      align-items: center;
    }
    .badge {
      display: inline-block;
      padding: 2px 10px;
      border-radius: 20px;
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
    }
    .badge-platform { background: #1f6feb; color: #e6edf3; }
    .badge-tier-live { background: #196c2e; color: #3fb950; }
    .badge-tier-archive7d { background: #9e6a03; color: #d29922; }
    .badge-tier-archive30d { background: #6e40c9; color: #bc8cff; }
    .badge-pain { background: #21262d; color: #8b949e; border: 1px solid #30363d; }
    .score-chip { font-size: 12px; color: #79c0ff; font-family: monospace; }

    .entry-body { margin-top: 10px; color: #8b949e; font-size: 13px; }

    /* Section header */
    .section-title {
      font-size: 13px;
      font-weight: 600;
      color: #8b949e;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      margin: 28px 0 14px;
      padding-bottom: 8px;
      border-bottom: 1px solid #21262d;
    }

    .footer {
      text-align: center;
      margin-top: 40px;
      color: #484f58;
      font-size: 12px;
    }

    @media (prefers-color-scheme: light) {
      body { background: #ffffff; color: #24292f; }
      .header { background: linear-gradient(135deg, #f6f8fa, #e8eaf6); border-color: #d0d7de; }
      .entry, .stat-card { background: #f6f8fa; border-color: #d0d7de; }
      .badge-pain { background: #f6f8fa; color: #57606a; }
    }
  </style>
</head>
<body>
<div class="wrapper">

  <!-- Header -->
  <div class="header">
    <h1>📡 OSS Radar Weekly Digest</h1>
    <div class="week-banner">Week of {{ date_str }}</div>
    {% set entries = report.entries or report.top_posts %}
    <p class="subtitle">{{ entries | length }} top signal{{ 's' if entries | length != 1 else '' }} from the past 7 days</p>
  </div>

  <!-- Stats row -->
  {% set platform_bd = report.platform_breakdown or {} %}
  {% set cat_bd = report.category_breakdown or {} %}
  <div class="stats-row">
    {% set entries = report.entries or report.top_posts %}
    <div class="stat-card">
      <div class="stat-value">{{ entries | length }}</div>
      <div class="stat-label">Top Posts</div>
    </div>
    <div class="stat-card">
      <div class="stat-value">{{ platform_bd | length }}</div>
      <div class="stat-label">Platforms</div>
    </div>
    <div class="stat-card">
      <div class="stat-value">{{ cat_bd | length }}</div>
      <div class="stat-label">Pain Categories</div>
    </div>
    <div class="stat-card">
      <div class="stat-value">{{ report.run_count or (report.daily_reports | length) }}</div>
      <div class="stat-label">Daily Runs</div>
    </div>
  </div>

  <!-- Top entries -->
  <div class="section-title">🏆 Top Signals This Week</div>
  {% set entries = report.entries or report.top_posts %}
  {% for post in entries %}
  {% set rank = loop.index %}
  <div class="entry">
    <div class="entry-header">
      <span class="rank-badge">
        {%- if rank == 1 %}🥇{%- elif rank == 2 %}🥈{%- elif rank == 3 %}🥉{%- else %}#{{ rank }}{%- endif %}
      </span>
      <a class="entry-title" href="{{ post.url }}">{{ post.title | truncate(90) }}</a>
    </div>
    <div class="entry-meta">
      <span class="badge badge-platform">{{ post.platform }}</span>
      {% set tier = post.source_tier or post.backfill_source or 'live' %}
      {% if tier == 'live' %}
        <span class="badge badge-tier-live">live</span>
      {% elif tier in ['archive-7d', 'archive_7d'] %}
        <span class="badge badge-tier-archive7d">archive-7d</span>
      {% elif tier in ['archive-30d', 'archive_30d'] %}
        <span class="badge badge-tier-archive30d">archive-30d</span>
      {% else %}
        <span class="badge badge-tier-live">live</span>
      {% endif %}
      {% for cat in post.pain_categories[:3] %}
        <span class="badge badge-pain">{{ cat.value if cat.value is defined else cat }}</span>
      {% endfor %}
      <span class="score-chip">⚡ {{ "%.2f"|format(post.final_score or post.signal_score) }}</span>
    </div>
    {% if post.llm_summary %}
    <div class="entry-body">{{ post.llm_summary }}</div>
    {% else %}
    {% set excerpt = (post.body or '')[:120] %}
    {% if excerpt %}
    <div class="entry-body">{{ excerpt }}{% if (post.body or '') | length > 120 %}…{% endif %}</div>
    {% endif %}
    {% endif %}
  </div>
  {% else %}
  <div class="entry" style="text-align:center; color:#484f58;">
    No qualifying posts found for this week.
  </div>
  {% endfor %}

  <!-- Platform breakdown -->
  {% if platform_bd %}
  <div class="section-title">📊 Platform Breakdown</div>
  <table style="width:100%;border-collapse:collapse;">
    {% for platform, count in platform_bd.items() %}
    <tr>
      <td style="padding:6px 0;color:#8b949e;font-size:13px;">{{ platform }}</td>
      <td style="padding:6px 0;text-align:right;color:#58a6ff;font-weight:600;">{{ count }}</td>
    </tr>
    {% endfor %}
  </table>
  {% endif %}

  <!-- Category breakdown -->
  {% if cat_bd %}
  <div class="section-title">🏷 Pain Category Distribution</div>
  <table style="width:100%;border-collapse:collapse;">
    {% for cat, count in cat_bd.items() %}
    <tr>
      <td style="padding:6px 0;color:#8b949e;font-size:13px;">{{ cat }}</td>
      <td style="padding:6px 0;text-align:right;color:#d2a8ff;font-weight:600;">{{ count }}</td>
    </tr>
    {% endfor %}
  </table>
  {% endif %}

  <div class="footer">
    <p>OSS Radar Weekly Digest &nbsp;·&nbsp; Week of {{ date_str }} &nbsp;·&nbsp; <a href="https://github.com">Unsubscribe</a></p>
  </div>
</div>
</body>
</html>
